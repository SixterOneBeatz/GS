import { Table } from '@mui/material';
import styled from 'styled-components';
const StyledTable = styled(Table)`
  width: ${props => props.width}px;
  height: ${props => props.height}%;
`;
export const TablaPedidosStyles = {
  StyledTable,
};
