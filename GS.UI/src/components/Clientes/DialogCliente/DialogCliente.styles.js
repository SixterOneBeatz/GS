import { Typography } from '@mui/material';
import styled from 'styled-components';

const StyledTitle = styled(Typography)`
  font-size: 14px;
`;

export const DialogClienteStyles = {
  StyledTitle,
};
